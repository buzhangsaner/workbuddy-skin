{
  "name": "workbuddy-dream-skin",
  "version": "0.6.0",
  "description": "Unofficial script-free skin manager and theme collection for WorkBuddy",
  "author": "buzhangsaner",
  "homepage": "https://github.com/buzhangsaner/workbuddy-skin",
  "private": true,
  "type": "module",
  "main": "manager/electron/main.cjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/injector.mjs && node --check assets/renderer-inject.js && node --check manager/electron/main.cjs && node --check manager/src/app.js",
    "manager:start": "electron .",
    "manager:dist:win": "electron-builder --win --x64 -p never",
    "manager:dist:mac": "electron-builder --mac --x64 --arm64 -p never"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.workbuddy.skinmanager",
    "productName": "WorkBuddy Skin Manager",
    "directories": {
      "output": "dist-manager"
    },
    "files": [
      "manager/**/*",
      "assets/**/*",
      "src/**/*",
      "scripts/**/*",
      "themes/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "assets/**/*",
      "src/**/*",
      "scripts/**/*",
      "themes/**/*"
    ],
    "win": {
      "target": ["nsis", "portable"]
    },
    "mac": {
      "category": "public.app-category.utilities",
      "target": ["dmg"],
      "artifactName": "WorkBuddy-Skin-Manager-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "WorkBuddy-Skin-Manager-Setup-${version}-${arch}.${ext}"
    },
    "portable": {
      "artifactName": "WorkBuddy-Skin-Manager-Portable-${version}-${arch}.${ext}"
    }
  }
}
