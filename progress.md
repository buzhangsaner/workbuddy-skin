# Progress

## 2026-07-16

- 完成本机 WorkBuddy 进程、安装包和主进程静态检查。
- 确认 CDP 技术路线可用，并发现官方环境变量入口。
- 确定独立项目架构、视觉范围和安全约束。
- 新增主题、安全与载荷测试；首次运行因三个模块缺失而按预期失败。
- 实现主题 schema、CDP URL/目标校验、进程身份比较和安全载荷构建。
- 完成 CDP 注入器、启动/恢复/验证与桌面快捷方式脚本。
- 完成单文件 Dream Skin Editor，并用本机 Chrome 生成实际渲染截图。
- 完成独立安装到 `%LOCALAPPDATA%\WorkBuddyDreamSkin\app`，三个桌面快捷方式均已验证存在。
- 在真实 WorkBuddy 5.2.6 完成启动、注入、截图、恢复原版、再次启动的闭环验证。
- 修复恢复竞态和 CDP 调用延迟；PowerShell 语法解析、Node 语法检查和 11 项自动测试全部通过。
- 新增“薛凯琪限定版”，改用真实图片图层解决 Chromium 超长 CSS Data URL 不显示问题，并通过 Windows 实机截图验证。
- 新增 macOS 独立安装、启动、恢复和编辑器 `.command` 入口，shell 语法检查通过。
- 针对实机反馈重新采集 WorkBuddy 5.2.6 DOM 与计算样式，确认旧 CSS 未命中 `.conversation-list-tab-button`、`.wb-scene-tabs__pill`、`.quick-actions__item` 等稳定类。
- 使用图像编辑生成无任何 UI、菜单、文字的纯人物星河素材 `hero-clean-v2.png`，移除主题目录中的旧截图背景。
- 为左侧导航、会话卡、用户菜单、场景按钮、快捷按钮、输入区和发送按钮增加明确主题状态，并完成实机截图与计算样式验证。
- Windows/macOS 启动流程改为先同步完成首轮注入，再启动后台守护，消除双击后短暂显示原版的竞态。
- 从 Codex Dream Skin 上游整理 8 套原版风格，全部改为无菜单、无按钮、无文字的纯 Hero 素材。
- 建立 `themes/catalog.json` 与 8 套声明式主题配置，实现 Windows Forms 和 macOS AppleScript 主题选择器。
- 热切换现会同步刷新已有 Hero 图片与名称，Windows PowerShell 5 的目录数组兼容问题已修复。
- 完成 8 套真实 WorkBuddy 截图；粉系、财神、红白、清透、灵感、紫夜、初音的原生控件色板均验证生效。
- 定位并覆盖黑金主题的 WorkBuddy 外层网格、主画布与左侧列表底色，实机复核为完整黑金界面。
- 启动右上角实时主题切换功能：已分析参考图，确定采用现有注入层内的胶囊按钮组，并记录原生主题与响应式交互边界。
- 新增主题目录 payload 与安全 `file://` 素材地址，避免 8 张图片重复 Base64 注入。
- 新增右上角 8 套主题加“恢复原主题”的 9 按钮玻璃胶囊栏，包含活动态、键盘焦点与窄屏滚动。
- 实机点击紫夜、初音、黑金与原生主题均即时生效；黑金经过 6.5 秒后台重注入后仍保持选择。
- Windows 完整关闭并重新启动 WorkBuddy 后，紫夜主题和对应活动按钮成功恢复，证明选择持久化有效。
- 修复后台热重注入的延迟任务句柄快照竞态；使用 CDP 真实鼠标按下/抬起点击财神主题，等待 6.2 秒后仍保持，并可再点击回紫夜。
- 最终使用真实鼠标事件遍历 8 套主题与“恢复原主题”全部成功；24 项测试、Node 语法、PowerShell/macOS shell 语法和安装副本一致性检查全部通过。
- 根据用户真实点击反馈，将主题栏从 `pointer-events:none` 的装饰根层彻底拆出，新增独立 `workbuddy-dream-switcher-root`，强制 `pointer-events:auto` 与 Electron `no-drag`。
- 将 Chat 欢迎页升级为原版整页沉浸式构图：全幅主题背景、品牌标题、状态胶囊、四张原生快捷卡和底部原生输入区。
- 修复旧装饰根热升级时品牌标题缺失，并完成 8 套主题实机截图与黑金卡片优先级复核。
- 根据实机反馈修正欢迎页重复侧栏偏移，使输入区与标题、卡片统一左边线；补齐 `.conversation-list`、任务/空间/会话项主题色。
- 对话页新增主题玻璃画布和主题输入框，自动隐藏首页叙事标题并降低背景人物干扰；紫夜与清透对话实机验证保留主题。
- 修复清透主题场景按钮与状态条重叠，将三个场景入口改为功能卡上方的紧凑分段玻璃栏，原功能保持可点。
- 右上角新增“自定义”入口和内置玻璃侧栏，支持背景图、四项色板、主题名称、主标题与英文眉题实时预览。
- 背景图仅接受 PNG/JPG/WebP，浏览器内缩放至最大 1920×1200 并压缩后保存；取消可恢复原主题，保存后选择切换为 `custom`。
- 在真实 WorkBuddy 中完成图片上传、标题/主色修改、5 秒热重注入、取消恢复、永久保存和完整退出重启验证；自定义主题与 10 个右上角按钮均成功恢复。
- 新增并观察场景选择器隐藏与 GitHub 入口测试按预期失败，随后以 CSS 隐藏原生 `.wb-scene-tabs`，不破坏 WorkBuddy DOM。
- 右上角主题栏末尾新增本地内联 SVG GitHub 图标，固定链接到 `buzhangsaner/workbuddy-skin`，包含安全新窗口属性、可访问标签、悬停和键盘焦点样式；定向 7 项测试通过。
- 安装 v0.4.0 到真实 WorkBuddy 后，确认 10 个主题按钮加 1 个 GitHub 图标、场景选择器 0×0 隐藏、图标真实鼠标命中、当前主题不变且页面无横向溢出；37 项全量测试和 Node 语法检查通过。
- 初始化本地 `main`，排除 22MB QA 截图，确认无 100MB 以上文件和常见密钥；通过 GitHub 官方设备授权登录 `buzhangsaner`，已将首个提交 `2b00dcd` 推送到 `buzhangsaner/workbuddy-skin`。
- 恢复欢迎页三个场景按钮，实现可持久化的 `—⌄` 主题栏收起拉手，并让热重注入保持状态。
- 将主题栏移到左侧菜单右边 10px，排列为“收起 → GitHub → 主题”；通过结构签名升级让旧安装自动重建 DOM。
- 修复黑金侧栏原生高特异性规则导致的黑字问题，实机标题为暖白、时间为金灰。
- 加入内置 Node 运行时选择、Windows Inno Setup、macOS App/DMG 和 GitHub Release 工作流；本地 45 项测试及脚本语法验证通过。
